import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ['api.unsplash.com', 'images.unsplash.com', 'plus.unsplash.com'],
  },
  /* config options here */
};

export default nextConfig;
